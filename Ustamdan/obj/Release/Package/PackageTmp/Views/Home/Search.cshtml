@model PagedList.IPagedList<PostViewModel>
@{
    ViewBag.Title = Resource.SearchTitle;
}

<section id="page-title" style="padding:25px 0">
    <div class="container clearfix text-center">
        <h3>Arama Sonuçları</h3>
    </div>
</section><!-- #page-title end -->
<!-- Content
============================================= -->
<section id="content">

    <div class="content-wrap">

        <div class="container clearfix">

            <!-- Post Content
            ============================================= -->
            <div class="postcontent nobottommargin clearfix">

                <!-- Posts
                ============================================= -->
                <div id="posts" class="post-grid grid-container grid-3 clearfix" data-layout="fitRows">
                    @foreach (var post in Model)
                    {
                        <div class="entry clearfix">
                            <div class="entry-image">
                                @if (post.PostType == PostType.Image)
                                {
                                    <a href="@post.MediaURL" data-lightbox="image"><img class="image_fade" src="@post.MediaURL" alt="@post.Title"></a>
                                }
                                else
                                {
                                    <iframe src="@post.MediaURL" width="500" height="281" frameborder="0" webkitallowfullscreen mozallowfullscreen allowfullscreen></iframe>
                                }
                            </div>
                            <div class="entry-title">
                                <h2><a href="@Url.Action("Post",new {id = post.Id })">@post.Title</a></h2>
                            </div>
                            <ul class="entry-meta clearfix">
                                <li><i class="icon-calendar3"></i> @post.DateCreated.ToShortDateString()</li>
                                <li>
                                    <i class="icon-folder-open"></i>
                                    @foreach (var cat in post.CategoryNames)
                                    {
                                        <a href="#">@cat</a>
                                    }
                                </li>
                            </ul>
                            <div class="entry-content">
                                <p>@MvcHtmlString.Create(new string(post.Body.Take(100).ToArray()))...</p>
                                <a href="@Url.Action("Post",new {id = post.Id })" class="more-link">Read More</a>
                            </div>
                        </div>
                    }

                </div><!-- #posts end -->
                <!-- Pagination
                ============================================= -->
                <ul class="pager nomargin">
                    <li class="previous"><a href="#">&larr; Older</a></li>
                    <li class="next"><a href="#">Newer &rarr;</a></li>
                </ul><!-- .pager end -->

            </div><!-- .postcontent end -->
            <!-- Sidebar
            ============================================= -->
        </div>

    </div>

</section><!-- #content end -->

